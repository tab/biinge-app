import { StyleSheet } from "react-native"

export const avatarStyles = StyleSheet.create({
  root: {
    overflow: "hidden",
  },
  image: {
    borderRadius: 50,
    height: "100%",
    width: "100%",
  },
})
